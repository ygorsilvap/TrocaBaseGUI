<Window x:Class="TrocaBaseGUI.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TrocaBaseGUI.Views"
        mc:Ignorable="d"
        Title="SettingsWindow" 
        Height="500" Width="550"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Border Background="#101e24" CornerRadius="20" BorderBrush="#2a353a" BorderThickness="1" Padding="6">

        <Grid Background="#101e24">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="57*"/>
                <RowDefinition Height="427"/>
            </Grid.RowDefinitions>
            <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" Background="Transparent" MouseDown="Border_MouseDown" Grid.RowSpan="3" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Image Source="/Assets/settings80.png" IsHitTestVisible="False" HorizontalAlignment="Left" Height="50" Margin="15,13,0,0" VerticalAlignment="Top" Width="45" Grid.RowSpan="2" Grid.Row="1"/>
            <TextBlock HorizontalAlignment="Left" IsHitTestVisible="False" Foreground="#f5f4f3" FontSize="26" FontWeight="Medium" Margin="75,18,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="200" Height="40" Grid.RowSpan="2" Grid.Row="1"><Run Language="pt-br" Text="Configurações"/><LineBreak/><Run Language="pt-br"/></TextBlock>
            <Button Click="CloseApp_Click" Content="✖" FontSize="22" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,13,24,0" BorderThickness="0" Foreground="#30444D"  Background="#101e24" Grid.Row="1">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                    Background="{TemplateBinding Background}"
                    CornerRadius="5"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter x:Name="contentPresenter"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  RecognizesAccessKey="True"
                                  TextElement.Foreground="{TemplateBinding Foreground}"
                                  TextElement.FontSize="{TemplateBinding FontSize}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="contentPresenter" Property="TextElement.Foreground" Value="#2a353a"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="contentPresenter" Property="TextElement.FontSize" Value="21"/>
                                <Setter TargetName="contentPresenter" Property="TextElement.Foreground" Value="#252E33"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Click="MinApp_Click" Content="➖" FontSize="22" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,13,75,0" BorderThickness="0" Foreground="#30444D"  Background="#101e24" Grid.Row="1">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                    Background="{TemplateBinding Background}"
                    CornerRadius="5"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter x:Name="contentPresenter"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  RecognizesAccessKey="True"
                                  TextElement.Foreground="{TemplateBinding Foreground}"
                                  TextElement.FontSize="{TemplateBinding FontSize}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="contentPresenter" Property="TextElement.Foreground" Value="#2a353a"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="contentPresenter" Property="TextElement.FontSize" Value="21"/>
                                <Setter TargetName="contentPresenter" Property="TextElement.Foreground" Value="#252E33"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Border BorderThickness="1" BorderBrush="#2a353a" Margin="0,35,0,60" CornerRadius="0, 0, 5, 5" Grid.Row="2" HorizontalAlignment="Center" Width="416"/>
            <TabControl Margin="60,9,60,392" SelectionChanged="TabControl_SelectionChanged" x:Name="TabControl" BorderThickness="0" Background="#101e24" Grid.Row="2">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border
                                Name="Border"
                                Background="#121f24"
                                BorderBrush="#2a353a"
                                BorderThickness="1"
                                CornerRadius="5,5,0,0"
                                Padding="8,4">
                                        <ContentPresenter x:Name="ContentSite"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Center"
                                                  ContentSource="Header"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#18262c"/>
                                            <Setter TargetName="Border" Property="BorderBrush" Value="#2a353a"/>
                                            <Setter Property="Foreground" Value="#f5f4f3"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#18262b"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>
                <TabItem Header="Conexão Local" Foreground="#f5f4f3" Margin="-2,0,2,0" />
                <TabItem Header="Conexão Server" Foreground="#f5f4f3" Margin="-2,0,2,0"/>
                <TabItem Header="Opc. 2 Camadas" Foreground="#f5f4f3" Margin="12,0,-12,0"/>
                <TabItem Header="Opc. 3 Camadas" Foreground="#f5f4f3" Margin="12,0,-12,0"/>
            </TabControl>
            <Button Content="Voltar" Click="VoltarButton_Click" Foreground="#f5f4f3" BorderBrush="#2a353a" Background="#101e24" Margin="220,382,220,10" Grid.Row="2">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                        Background="{TemplateBinding Background}"
                        CornerRadius="5"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                        VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#18262b"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#2a353a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</Window>

